{
  "name": "earthseed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "patch-package && vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "postinstall": "patch-package",
    "relay:luke": "./switch-relay.sh luke",
    "relay:cloudflare": "./switch-relay.sh cloudflare",
    "relay:luke:deploy": "./switch-relay.sh luke --deploy",
    "relay:cloudflare:deploy": "./switch-relay.sh cloudflare --deploy",
    "relay:status": "grep 'const RELAY_SERVER' src/main.ts | head -1"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "vite": "^7.3.0",
    "wrangler": "^4.54.0"
  },
  "dependencies": {
    "patch-package": "^8.0.1",
    "@kixelated/hang": "^0.7.0",
    "@kixelated/libavjs-webcodecs-polyfill": "^0.5.5",
    "@kixelated/web-transport-ws": "^0.1.2"
  }
}
